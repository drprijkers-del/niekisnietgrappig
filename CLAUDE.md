# Project Rules

## Deployment
- **NOOIT deployen zonder lokaal te testen.** Altijd eerst lokaal draaien (`npm run dev` + browser/curl check), dan `npm run build`, dan pas committen en deployen. Geen uitzonderingen.
